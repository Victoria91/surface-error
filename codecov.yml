coverage:
  status:
    project:
      default:
        target: 55% # the required coverage value
        threshold: 1% # the leniency in hitting the target
    patch:
      default:
        target: auto
        threshold: 1%
